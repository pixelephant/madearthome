<p id="notice"><%= notice %></p>

<% @cart.line_items.each do |li| %>
<%= li.product.name << ' - ' << li.quantity.to_s << ' || ' %>
<% end %>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
